# 複製這份成 line-lab/.env。
# U3 主線是真 LINE OA Flex 推播，老師會提供課堂用 LINE OA 設定值。
# 注意：改完 .env 要重新啟動 npm run dev，伺服端才會重新讀取。
# .env 不會被 commit；token 不要貼到前端、簡報、報告或 Git。
LINE_CHANNEL_ACCESS_TOKEN=老師提供的 channel access token
LINE_TARGET_ID=老師提供的 userId 或 groupId
LINE_REAL_SEND=1
